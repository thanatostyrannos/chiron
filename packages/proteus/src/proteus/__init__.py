"""Proteus — the configurable model architecture.

Decoder, attention variants (full / SWA / linear / SSM), MoE gating, positional
schemes. Every ablation axis is a config field; the config surface is the
experimental surface (docs/proteus-config-space.md, authored in the Rig Design phase).
"""

__version__ = "0.1.0"
