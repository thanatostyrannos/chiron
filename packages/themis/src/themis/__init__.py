"""Themis — the ablation rig.

Turns "does change X help?" into evidence: pre-registered G2 hypothesis cards,
matched param/token budgets, seed management (>=3), run execution, aggregation,
and reporting with confidence intervals. Submodules: argus (telemetry), data (loaders).
"""

__version__ = "0.1.0"
