"""Data loaders for Themis.

Seeded, resumable dataloaders over a small permissive corpus, plus the targeted
recall/retrieval probes (docs/corpus-and-probes.md). A dataloader is `data/`, not a
demigod — it stays unnamed until it earns an interface and a lifecycle of its own.
"""
