# ASSUMPTIONS — running ledger (G2)

A **register** (see CLAUDE.md → DOCUMENT CLASSES): rows are appended and their
**status** is updated; rows are never deleted. Every assumption baked into the
kickoff starts here. Update at every Definition of Done.

Status ∈ `untested` / `supported` / `refuted`. Evidence tag ∈ `[M]` measured
(run ID + seed count or preflight), `[C]` cited (arXiv/URL + date), `[A]` assumed
(state confidence + the cheapest test that would move it). Never state an `[A]` in
the register of an `[M]`.

| # | Assumption | Status | Evidence | Date |
|---|---|---|---|---|
| hardware-capacity-ceiling | The gfx1151 UMA pool yields a usable single-allocation ceiling near the BIOS UMA FB Size (target 96 GB), and that number bounds every long-context / KV-capacity experiment. | **partially measured; mechanism refuted** | `[M]` 2026-07-26: **≥74.40 GiB written, read back and released in 4.04 s** with no paging collapse — that memory demonstrably works. Reported pool 82.99 GiB; allocation-only probes returned ≥100 GiB, so the driver oversubscribes into system RAM. The **exact** writable ceiling is still unmeasured: two probes disagreed (82.67 vs 100) and both were reporting their own search bounds, neither having written to the memory. Separately, the driver reports only **16 GiB dedicated VRAM** — BIOS UMA FB Size is at default, not 96 GB — so the pool is reached through GTT/shared memory, not the carve-out. The stated link to BIOS UMA FB Size is wrong as written. | 2026-07-26 |
| gpu-fast-tier-size | The GPU has a fast memory tier smaller than its pool, and the BIOS UMA carve-out sets its size. This — not the pool size — is the number that sizes KV-cache experiments. | **supported** | `[M]` 2026-07-26, `notebook/uma-carveout-controls-fast-tier.md`, single run per arm: with 16 GiB dedicated the fast tier ended at a **30 GiB** footprint (61 GB/s at 32 GiB); with **96 GiB** dedicated it is flat at ~200 GB/s out to **≥62 GiB** with no degradation. Pre-registered SUCCESS was ≥60 GiB. **Budget long-context experiments against ≥62 GiB.** The upper edge is unmeasured — the sweep hit the fault below, not a bandwidth knee. | 2026-07-26 |
| large-tensor-fault-32gib | Single tensors ≥32 GiB are safe to allocate and operate on. | **refuted** | `[M]` 2026-07-26: a 31 GiB buffer copies cleanly at 199.9 GB/s; a **32 GiB** buffer hard-hangs (11 min at 0 CPU, host free RAM to 5 GB, force-killed); a 36 GiB buffer raises `hipErrorLaunchFailure`. GPU recovers fully in a fresh process. `[A]` medium confidence this is a 32-bit overflow in the copy path, not a capacity limit — 32 GiB is exactly 2^35 bytes / 2^34 fp16 elements. Cheapest test: 32 GiB as fp32 (same bytes, half the elements) — failure means bytes, success means element count. **A hang at 0 CPU is silent**, so this can stop a long run without crashing it. Hardware Validation Gate item. | 2026-07-26 |
| torch-build | The lab's venv will run AMD gfx1151 ROCm nightly torch; the system Python's `torch 2.11.0+cu128` (CUDA) is irrelevant to the lab and will not be used. | **supported** | `[M]` 2026-07-26: `C:\venvs\lab` runs `torch 2.12.0a0+rocm7.13.0a20260313` (HIP 7.2.0), `torch.cuda.is_available()==True`, device `AMD Radeon(TM) 8060S Graphics`, arch `gfx1151`. Note the torch wheel **pins** `rocm==7.13.0a20260313`, downgrading the 7.14.0a20260612 SDK installed first — the pinned pair is the lab's instrument. | 2026-07-26 |
| native-windows-over-wsl2 | Native Windows is the correct ROCm target; WSL2 clamps the ROCm pool to the `.wslconfig` memory value and cannot reach dedicated VRAM. | untested (locally) | `[C]` ROCm issue #6022 (librocdxg VRAM mapping under WSL2). Decisive local test: allocate > `.wslconfig` limit and check whether it reaches the dedicated pool. | 2026-07-24 |
| bf16-numerics-unproven | bf16 on gfx1151 is numerically trustworthy for matmul / softmax / RMSNorm / attention at the shapes we use. | untested | `[C]` five critical bf16 bugs documented on gfx1151 (ROCm #6034 / amdsense). Test: Hardware Validation Gate numerics suite (bf16 vs fp32 reference, max abs/rel error). | 2026-07-24 |
| single-device-only | Distributed collectives are incomplete on gfx1151, so all training is single-device; FSDP/DDP/TP/PP/EP are design-only until rented hardware validates them. | supported (as constraint) | `[C]` `torch._C._distributed_c10d` incomplete on gfx1151 (CLAUDE.md, ROCm docs). | 2026-07-24 |
| hipblaslt-config | GEMM throughput requires `HIPBLASLT_TENSILE_LIBPATH` set and `TORCH_BLAS_PREFER_HIPBLASLT=1`; a bad path drops GEMM ~5x (≈33→≈6 TFLOPS). | **refuted as stated (the 5x cliff); the setting still helps** | `[C]` ROCm #6022. `[M]` 2026-07-26, `scripts/benchmark_gemm.py` at 8192³: **unset 18.6 TFLOPS bf16 → configured 20.9 TFLOPS bf16** (+12%), fp16 19.0 → 19.8. No 5x cliff on this wheel — the pip ROCm SDK evidently finds usable kernels without the env var. Set it anyway via `scripts/activate-lab.ps1`; it is free and the cliff may return on another wheel. | 2026-07-26 |
| gemm-throughput-below-reference | Our GEMM reaches the ~33 TFLOPS reported for this silicon. | **refuted** | `[M]` 2026-07-26: 20.9 TFLOPS bf16 at 8192³ best case = **63% of the cited figure** `[C]` (ROCm #6022). Unexplained. Candidates: tablet power/thermal limits, a different wheel or kernel set, or the cited number being measured differently. Matters for cost models, not for correctness. Cheapest test: repeat on AC power in the highest performance profile and compare. | 2026-07-26 |
| z13-is-right-instrument | A capacity/bandwidth-bound research agenda (memory systems) is better served by 128 GB unified memory than by a 20 GB discrete card, despite slower wall-clock. | supported | `[C]` 93-experiment campaign on this silicon measured 25% MFU vs 7.7% on an RTX 4090 on a memory-bound recipe (ROCm #6034, Mar 2026). | 2026-07-24 |
| ablation-scale-sufficient | 20M–300M params on 0.5–5B tokens is enough to answer the memory-systems questions we care about (recall cliffs, eviction/recall tradeoffs, hybrid ratios). | untested | `[A]` medium confidence, from kickoff design. Test: the Ablation Backlog's first hypothesis producing a decision-changing result at this scale. | 2026-07-24 |
| cloud-budget-zero | Starting `CLOUD_BUDGET = $0`; all work runs on the Z13, renting H100 hours only when a specific result justifies it. | supported | `[A]` user-set 2026-07-24. Contribution-margin (info/$) ranks any future spend (see G3 translation). | 2026-07-24 |
| mnemosyne-separable | Mnemosyne (memory subsystem) is mechanically separable from Proteus — it never imports proteus/themis — which is what makes it a contribution rather than an implementation detail. | supported (mechanism in place) | `[C]` enforced by `packages/mnemosyne/pyproject.toml` (no proteus dep) + `tests/test_package_boundaries.py` lint contract, scaffolded 2026-07-24. `[M]` 2026-07-26: contract proved red-then-green — a transient `import proteus` under `packages/mnemosyne/src/mnemosyne/` fails the test naming the offending file; removing it passes. The guard is real, not decorative. Acceptance test (clean-venv wheel) still runs at the mnemosyne-core milestone. | 2026-07-26 |
| reference-model | Poolside Laguna S 2.1 (118B-A8.5B MoE, mixed SWA/global attention, OpenMDW-1.1) is the primary reference architecture under study. | supported | `[C]` model card / configs (to be fetched in Reference Library phase). | 2026-07-24 |
| gfx1151-windows-wheels-exist | AMD ships gfx1151 ROCm nightly PyTorch wheels for native Windows via the rocm.nightlies index. | **supported** | `[M]` 2026-07-26: installed `torch-2.12.0a0+rocm7.13.0a20260313-cp312-cp312-win_amd64.whl` (631 MB) plus torchvision/torchaudio and `rocm-sdk-libraries-gfx1151` from `https://rocm.nightlies.amd.com/v2/gfx1151/`. Works on native Windows with no `/opt/rocm` system install. | 2026-07-26 |
| toolchain-present | git, gh (authenticated), Python 3.12, uv are installed and adequate for scaffold/design work. | supported | `[M]` preflight 2026-07-24: all green. | 2026-07-24 |

## Open blockers derived from the above

- ~~**`hardware-capacity-ceiling` is unmeasured.**~~ Filled 2026-07-26: **82.67 GiB**
  usable single allocation, 74.40 GiB held and read back. The gfx1151 stack is live on
  native Windows.
- ~~**The number that actually binds is smaller than the ceiling.**~~ Resolved
  2026-07-26 by `notebook/uma-carveout-controls-fast-tier.md`: BIOS UMA FB Size set to
  96 GB, fast tier moved from 30 GiB to **≥62 GiB** at ~200 GB/s. Budget long-context
  experiments against 62 GiB.
- **Single tensors ≥32 GiB hang or fault** (`large-tensor-fault-32gib`). Keep individual
  buffers under 32 GiB until the Hardware Validation Gate characterises it. The hang
  presents at 0 CPU with no error, so a long run would stall rather than fail.
- **Numerics remain unproven** (`bf16-numerics-unproven`). The Hardware Validation Gate
  has not run. No result from this machine counts as evidence yet.
